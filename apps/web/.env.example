PUBLIC_SERVER_HOST="http://localhost:3000"

# Public site URL (no trailing slash). Used at build time for og:image / twitter:image.
# On Vercel this is inferred from VERCEL_URL when unset.
PUBLIC_SITE_ORIGIN="https://dev-chess-d.netlify.app"
