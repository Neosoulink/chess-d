{
	"$schema": "https://turborepo.org/schema.json",
	"extends": ["//"],
	"tasks": {
		"dev": {
			"env": ["CLIENT_HOST"]
		},
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**"],
			"env": ["CLIENT_HOST"]
		}
	}
}
