# Must match the web app origin (scheme + host + port). `main.ts` also allows the
# localhost ↔ 127.0.0.1 swap automatically. Comma-separate more origins if needed.
CLIENT_HOST="http://localhost:5173"
# EXTRA_CORS_ORIGINS="http://192.168.1.10:5173"

# Optional — LLM chess AI proxies (`/ai/llm/*`). Keys stay server-side only.
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# OPENAI_MODEL=gpt-4o-mini
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-2.0-flash
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
